# Device packages

Use `tools/add-device` and follow [the API guide](../docs/ADAPTER-API.md).
One exact model per directory; drafts are inert. Existing owner models remain
on their original bridges, pins and captures. Do not copy their evidence.
