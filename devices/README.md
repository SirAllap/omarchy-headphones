# Add your headphones

Run `tools/add-device` from an isolated checkout. Each model gets its own
directory here. See [the device API guide](../docs/ADAPTER-API.md).

An empty package is a work in progress, never a claim of support. The checker
lists missing evidence and fails until the package is ready. Existing models
and their owner pins keep their original locations and runtime behavior.
